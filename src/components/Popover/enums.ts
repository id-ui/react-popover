export enum PopoverTriggerType {
  click = 'click',
  hover = 'hover',
  contextMenu = 'contextMenu',
  focus = 'focus',
}

export enum ArrowPlacementType {
  horizontal = 'horizontal',
  vertical = 'vertical',
}

export enum ArrowPlacement {
  center = 'center',
  top = 'top',
  bottom = 'bottom',
  right = 'right',
  left = 'left',
}

export enum PopoverPlacement {
  top = 'top',
  topRight = 'topRight',
  topLeft = 'topLeft',
  bottom = 'bottom',
  bottomRight = 'bottomRight',
  bottomLeft = 'bottomLeft',
  right = 'right',
  rightTop = 'rightTop',
  rightBottom = 'rightBottom',
  left = 'left',
  leftTop = 'leftTop',
  leftBottom = 'leftBottom',
}
